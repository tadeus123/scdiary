{
  "name": "me",
  "title": "Me",
  "description": "Return the signed-in Airsup20 user, whether setup is required, listing summary, active intents, and unread inbox count. Call this first when unsure about setup state, and whenever the user asks who they are on Airsup20.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "include_listing": {
        "type": "boolean",
        "default": true,
        "description": "Include listing summary in the response."
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "ok": { "type": "boolean" },
      "needs_setup": { "type": "boolean" },
      "inbox_unread": { "type": "number" },
      "trace_id": { "type": "string" },
      "note": { "type": "string" }
    },
    "required": ["ok", "trace_id"],
    "additionalProperties": true
  },
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
