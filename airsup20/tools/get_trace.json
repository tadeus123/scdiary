{
  "name": "get_trace",
  "description": "Inspect accurate timing for an Airsup20 tool call. Pass trace_id from a previous tool result, or omit to list recent traces. Spans show where time was spent (db, probe, endpoint, etc.).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "trace_id": {
        "type": "string",
        "description": "Trace id returned by a previous tool call."
      }
    },
    "additionalProperties": false
  }
}
