{
  "name": "update_supplier_card",
  "title": "Update supplier card",
  "description": "Update capabilities, names, city, niche, context, goal, or contact fields. Live factories are locked unless force:true. Resolve by company_id or exact domain only.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "company_id": { "type": "string" },
      "domain": { "type": "string" },
      "force": { "type": "boolean", "description": "Required to edit a live factory" },
      "company_name": { "type": "string" },
      "company_name_en": { "type": "string" },
      "website": { "type": "string" },
      "city": { "type": "string" },
      "niche": { "type": "string" },
      "context": { "type": "string" },
      "goal": { "type": "string" },
      "contact_name": { "type": "string" },
      "contact_email": { "type": "string" },
      "actions": { "type": "array", "items": { "type": "string" } },
      "profile": { "type": "object", "additionalProperties": true }
    },
    "additionalProperties": false
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": true,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
