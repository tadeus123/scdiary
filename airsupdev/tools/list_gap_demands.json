{
  "name": "list_gap_demands",
  "title": "List gap demands",
  "description": "List unmatched buyer needs where no live Airsup factory could fulfill the request. Use before drafting gap outreach. Never invent RFQs — only real recorded gaps.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "status": { "type": "string", "enum": ["open", "outreach", "filled", "closed"] },
      "limit": { "type": "integer", "minimum": 1, "maximum": 100 }
    },
    "additionalProperties": false
  },
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
