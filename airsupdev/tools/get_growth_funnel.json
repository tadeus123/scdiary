{
  "name": "get_growth_funnel",
  "title": "Get growth funnel",
  "description": "Ops funnel: sent → bounced/delivered → human claim open → published → live. Open counts are human-only; scanner GETs are not opens.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  },
  "annotations": {
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
