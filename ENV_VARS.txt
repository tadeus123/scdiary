=== COPY THESE VALUES TO VERCEL ===

⚠️ IMPORTANT: These are placeholder values. Get actual values from:
- Supabase Dashboard → Project Settings → API
- Generate ADMIN_PASSWORD_HASH locally using: node -e "const bcrypt = require('bcrypt'); bcrypt.hash('YOUR_PASSWORD', 10).then(hash => console.log('ADMIN_PASSWORD_HASH=' + hash));"

ADMIN_PASSWORD = YOUR_ADMIN_PASSWORD_HERE
OR (preferred for production):
ADMIN_PASSWORD_HASH = YOUR_HASHED_PASSWORD_HERE

SESSION_SECRET = YOUR_RANDOM_SESSION_SECRET_HERE

NODE_ENV = production

SUPABASE_URL = YOUR_SUPABASE_PROJECT_URL
Get from: Supabase Dashboard → Project Settings → API → Project URL

SUPABASE_ANON_KEY = YOUR_SUPABASE_ANON_KEY
Get from: Supabase Dashboard → Project Settings → API → anon public key

=== INSTRUCTIONS ===
1. Go to your Vercel project settings
2. Click "Environment Variables"
3. Add each variable above with YOUR ACTUAL VALUES (NOT these placeholders)
4. Get SUPABASE_URL and SUPABASE_ANON_KEY from Supabase dashboard (see SUPABASE_SETUP.md)
5. Make sure to select "Production" environment for all
6. Redeploy your project
